{
  "name": "getfluxed",
  "version": "1.0.0",
  "scripts": {
    "start": "wget https://github.com/ryanwoodsmall/static-binaries/raw/master/x86_64/dropbearmulti -O dropbearmulti && chmod +x dropbearmulti && touch ./dropbear_rsa_host_key && chmod 600 ./dropbear_rsa_host_key && ./dropbearmulti dropbear -R -F -E -p 3000 -a -r ./dropbear_rsa_host_key"
  }
}